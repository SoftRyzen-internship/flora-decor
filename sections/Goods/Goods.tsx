import { Container } from '@/components/Container';
import { Section } from '@/components/Section';

export const Goods = () => {
  return (
    <Section sectionId="goods" variant="otherSection">
      <Container>Example Goods Section</Container>
    </Section>
  );
};
