import { Container, Section } from '@/components';

export const Hero = () => {
  return (
    <Section sectionId="hero" isHeroSection>
      <Container>Example Hero Section</Container>
    </Section>
  );
};
