import { Container } from '@/components/Container';
import { Section } from '@/components/Section';

export const Hero = () => {
  return (
    <Section sectionId="hero" variant="heroSection">
      <Container>Example Hero Section</Container>
    </Section>
  );
};
